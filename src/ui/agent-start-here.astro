---
import CodeBlock from "./code-block.astro";
import { UI_SKILLS_AGENT_START_COPY } from "../lib/ui-skills-cli";
---

<div class="bg-fill-strong/40 rounded-[12px] p-4 sm:p-5">
  <h2 class="text-content-primary text-base font-medium text-balance">
    Agent, start here.
  </h2>
  <p
    class="text-content-secondary mt-3 max-w-2xl text-base leading-snug text-pretty"
  >
    You are working on a UI task. Before reading or changing code, run:
  </p>
  <div class="mt-4 flex w-full">
    <CodeBlock
      className="w-full"
      variant="stacked"
      code={UI_SKILLS_AGENT_START_COPY}
      language="bash"
      copyContent={UI_SKILLS_AGENT_START_COPY}
    />
  </div>

  <p
    class="text-content-secondary mt-4 max-w-2xl text-base leading-snug text-pretty"
  >
    Start with <code class="text-[0.875em]">npx ui-skills start</code>. Load the
    smallest relevant skill, then implement. Do not choose manually from the
    collection below.
  </p>
</div>
